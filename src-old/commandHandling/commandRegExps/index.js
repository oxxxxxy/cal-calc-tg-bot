const ru = require(`./languages/ru.js`);
const en = require(`./languages/en.js`);
const multiLang = require(`./languages/multiLang.js`);

const makeObjOfCommandsREByInputTypeStr = inputTypeStr => {};
const keysOfCommandRE = Object.keys(ru);

// messageREs.HELP(languageCode);


// subprocessMessage.ADD_INGREDIENT(languageCode);
